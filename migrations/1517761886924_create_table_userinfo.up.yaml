- args:
    sql: CREATE TABLE "userinfo"("id" serial NOT NULL, "hasura_id" integer NOT NULL,
      "name" text NOT NULL, "email" text NOT NULL, "gender" text NOT NULL, "profile_file_id"
      text NOT NULL, "age" integer NOT NULL, "about_me" text NOT NULL, "city" text
      NOT NULL, PRIMARY KEY ("hasura_id") )
  type: run_sql
- args:
    name: userinfo
  type: add_existing_table_or_view
